{
  "name": "sabd-studio",
  "version": "1.0.0",
  "description": "Unified Sabd Studio frontend and Django API monorepo",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint"
  },
  "dependencies": {
    "next": "14.1.4"
  }
}
